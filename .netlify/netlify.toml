# Netlify config removed/cleared
# This file was previously used for Netlify deployments. It has
# been cleared to avoid Netlify-specific behavior during local
# development (npm run dev / npm preview).
